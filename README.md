install requirements from requirements.txt:
pip install -r requirements.txt


test footage is added so no path need to be added for footage , if need to change footage change it in .env file

first run app.py and then streamlit in order:

To run app.py:
python app.py 

To run streamlit:
python -m streamlit run dashboard/app.py  
